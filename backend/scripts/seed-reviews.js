const mongoose = require('mongoose');
const path = require('path');
const Review = require('../src/models/Review');
const User = require('../src/models/User');
const Photographer = require('../src/models/Photographer');
const Booking = require('../src/models/Booking');

// Load .env from parent directory
require('dotenv').config({ path: path.join(__dirname, '..', '.env') });

// Random Arabic names
const clientNames = [
  'فاطمة أحمد', 'نورة محمد', 'سارة عبدالله', 'مريم خالد', 'هند سعيد',
  'ريم عبدالعزيز', 'لينا حسن', 'دانة علي', 'شهد يوسف', 'جود إبراهيم',
  'لمى عمر', 'غلا سلطان', 'رهف ناصر', 'جنى فهد', 'ملاك عبدالرحمن',
  'أسماء طارق', 'بشاير راشد', 'منى سليمان', 'عبير ماجد', 'وعد فيصل'
];

// Sample review comments in Arabic
const reviewComments = [
  'تجربة رائعة جداً! المصورة محترفة وملتزمة بالمواعيد. الصور جميلة جداً وأعجبتني كثيراً. أنصح الجميع بالتعامل معها.',
  'ما شاء الله تبارك الله، صور احترافية وجودة عالية. المصورة ذوق راقي وتعاملها ممتاز. شكراً جزيلاً على الخدمة الرائعة.',
  'صراحة فوق التوقعات! الصور طلعت أجمل مما تخيلت. المصورة فنانة وعندها حس إبداعي عالي. الله يوفقها.',
  'تجربة ممتازة من البداية للنهاية. المصورة متعاونة جداً وصبورة. الصور جاءت بجودة عالية وتسليم سريع.',
  'أفضل مصورة تعاملت معها! محترفة وملتزمة وذوقها راقي. الصور خيالية وأسعارها معقولة. بالتوفيق دائماً.',
  'تجربة جميلة ومريحة. المصورة لطيفة وتعرف كيف تخلي الجلسة ممتعة. الصور طلعت رهيبة وأعجبت الجميع.',
  'ما شاء الله عليها، شغل نظيف واحترافي. الصور جودتها عالية والتعديل ممتاز. أكيد بتعامل معها مرة ثانية.',
  'مصورة موهوبة وعندها خبرة واضحة. الصور جميلة جداً والإضاءة والزوايا كلها مدروسة. شكراً على الإبداع.',
  'تجربة رائعة! المصورة فاهمة شغلها وعندها أفكار إبداعية. الصور طلعت أحلى من المتوقع. الله يبارك فيها.',
  'صراحة ممتازة! من أفضل المصورات اللي تعاملت معهم. الصور احترافية والتسليم في الوقت المحدد. شكراً جزيلاً.',
  'تجربة جميلة وسلسة. المصورة محترفة وتعاملها راقي. الصور جودتها عالية وأسعارها مناسبة. أنصح بها بقوة.',
  'ما شاء الله، شغل فني راقي! المصورة عندها حس إبداعي وتعرف كيف تطلع أحلى ما فيك. الصور خيالية.',
  'تجربة ممتعة ومريحة. المصورة لطيفة وصبورة وتعطيك وقتك. الصور طلعت جميلة جداً وبجودة عالية.',
  'أفضل قرار اتخذته! المصورة محترفة وملتزمة والصور فاقت التوقعات. شكراً على الخدمة الرائعة.',
  'تجربة رائعة من كل النواحي. المصورة فنانة وعندها ذوق راقي. الصور جميلة جداً والتعديل احترافي.',
  'ما شاء الله عليها، شغل متقن واحترافي. الصور جودتها ممتازة والتسليم سريع. بالتوفيق دائماً.',
  'مصورة موهوبة جداً! الصور طلعت أجمل مما تخيلت. تعاملها ممتاز وأسعارها معقولة. أنصح الجميع بها.',
  'تجربة جميلة ومميزة. المصورة محترفة وعندها خبرة واضحة. الصور رهيبة والجودة عالية جداً.',
  'صراحة فوق الممتاز! المصورة فاهمة شغلها وعندها أفكار إبداعية. الصور خيالية وأعجبت الجميع.',
  'أفضل تجربة تصوير مريت فيها! المصورة محترفة وملتزمة والصور احترافية. شكراً جزيلاً على كل شيء.',
];

// Sample photographer replies
const photographerReplies = [
  'شكراً جزيلاً على كلماتك الجميلة! سعيدة جداً بإعجابك بالصور. دائماً في خدمتك 💕',
  'الله يسعدك ويحفظك! شكراً على ثقتك وتعاملك الراقي. أتمنى أشوفك قريب في جلسة جديدة 🌸',
  'ما شاء الله عليك! كلماتك أسعدتني كثير. شكراً على التعاون والذوق الراقي. بالتوفيق دائماً ✨',
  'شكراً حبيبتي على التقييم الجميل! سعيدة إني قدرت أحقق توقعاتك. دائماً في الخدمة 💖',
  'الله يخليك ويسعدك! شكراً على كلماتك اللطيفة. كان شرف لي التعامل معك. بالتوفيق 🌹',
  'شكراً من القلب! سعيدة جداً بإعجابك. تعاملك كان راقي وممتع. أتمنى نتعاون مرة ثانية 💕',
  'الله يبارك فيك! كلماتك أسعدتني كثير. شكراً على الثقة والتعاون. دائماً في خدمتك 🌸',
  'شكراً حبيبتي! سعيدة إن الصور عجبتك. كان ممتع التعامل معك. بالتوفيق دائماً ✨',
  'ما شاء الله عليك! شكراً على التقييم الجميل والكلمات اللطيفة. أتمنى أشوفك قريب 💖',
  'الله يسعدك! شكراً جزيلاً على ثقتك وتعاملك الراقي. دائماً في الخدمة 🌹',
  'شكراً من القلب على كلماتك الجميلة! سعيدة جداً بإعجابك. بالتوفيق دائماً 💕',
  'الله يخليك! شكراً على التقييم الرائع. كان شرف لي التعامل معك. أتمنى نتعاون مرة ثانية 🌸',
  'شكراً حبيبتي على الكلمات اللطيفة! سعيدة إني قدرت أحقق توقعاتك. دائماً في خدمتك ✨',
  'ما شاء الله! شكراً جزيلاً على ثقتك. تعاملك كان راقي وممتع. بالتوفيق دائماً 💖',
  'الله يبارك فيك! كلماتك أسعدتني كثير. شكراً على التعاون الجميل. دائماً في الخدمة 🌹',
  'شكراً من القلب! سعيدة جداً بإعجابك بالصور. أتمنى أشوفك قريب في جلسة جديدة 💕',
  'الله يسعدك ويحفظك! شكراً على التقييم الرائع والكلمات الجميلة. بالتوفيق دائماً 🌸',
  'شكراً حبيبتي! سعيدة إن الصور عجبتك. كان ممتع التعامل معك. دائماً في خدمتك ✨',
  'ما شاء الله عليك! شكراً جزيلاً على ثقتك وتعاملك الراقي. أتمنى نتعاون مرة ثانية 💖',
  'الله يخليك! شكراً على الكلمات الجميلة. كان شرف لي التعامل معك. بالتوفيق دائماً 🌹',
];

// Connect to MongoDB
const connectDB = async () => {
  try {
    const mongoUri = process.env.MONGODB_URI || process.env.MONGO_URI;
    
    if (!mongoUri) {
      console.error('❌ MONGODB_URI not found in .env file');
      console.log('💡 Please make sure .env file exists in backend directory');
      process.exit(1);
    }
    
    console.log('🔌 Connecting to MongoDB...');
    await mongoose.connect(mongoUri);
    console.log('✅ MongoDB connected');
  } catch (error) {
    console.error('❌ MongoDB connection error:', error);
    process.exit(1);
  }
};

// Seed reviews
const seedReviews = async () => {
  try {
    console.log('🌱 Starting to seed reviews...');

    // Get a photographer
    const photographer = await Photographer.findOne().populate('user');
    if (!photographer) {
      console.error('❌ No photographer found. Please create a photographer first.');
      return;
    }

    console.log(`📸 Found photographer: ${photographer.user.name}`);

    // Create 20 reviews with different clients
    const reviews = [];
    const createdClients = [];
    
    for (let i = 0; i < 20; i++) {
      // Create a unique client for each review
      const clientName = clientNames[i];
      const clientEmail = `client${i + 1}@test.com`;
      
      let client = await User.findOne({ email: clientEmail });
      if (!client) {
        client = await User.create({
          name: clientName,
          email: clientEmail,
          password: 'password123',
          phone: `+96650000${String(i).padStart(4, '0')}`,
          role: 'client',
        });
        createdClients.push(client);
      }
      
      // Random rating between 3 and 5 (more realistic)
      const randomValue = Math.random();
      let rating;
      if (randomValue > 0.7) {
        rating = 5; // 30% chance
      } else if (randomValue > 0.3) {
        rating = 4; // 40% chance
      } else {
        rating = 3; // 30% chance
      }
      
      // Create a completed booking first
      const booking = await Booking.create({
        client: client._id,
        photographer: photographer._id,
        date: new Date(Date.now() - Math.random() * 30 * 24 * 60 * 60 * 1000), // Random date in last 30 days
        timeSlot: '09:00 - 11:00',
        location: 'الرياض',
        status: 'completed',
        payment: {
          amount: 500,
          status: 'paid',
        },
      });

      // Create review
      const review = await Review.create({
        booking: booking._id,
        client: client._id,
        photographer: photographer._id,
        rating: rating,
        comment: reviewComments[i],
        createdAt: new Date(Date.now() - Math.random() * 25 * 24 * 60 * 60 * 1000), // Random date
      });

      // Add photographer reply (90% chance - more realistic)
      const hasReply = Math.random() > 0.1;
      if (hasReply) {
        review.photographerReply = {
          comment: photographerReplies[i],
          createdAt: new Date(review.createdAt.getTime() + Math.random() * 3 * 24 * 60 * 60 * 1000), // 0-3 days after review
        };
        await review.save();
      }

      reviews.push(review);
      console.log(`✅ Review ${i + 1}/20 - ${rating} ⭐ - ${clientName} ${hasReply ? '(مع رد)' : '(بدون رد)'}`);
    }
    
    console.log(`\n👥 Created ${createdClients.length} new clients`);
    console.log(`📝 Created ${reviews.length} reviews`);

    // Update photographer rating
    const allReviews = await Review.find({ photographer: photographer._id });
    const totalRating = allReviews.reduce((sum, r) => sum + r.rating, 0);
    const averageRating = totalRating / allReviews.length;

    photographer.rating = {
      average: averageRating,
      count: allReviews.length,
    };
    await photographer.save();

    console.log('\n🎉 Successfully seeded 20 reviews!');
    console.log(`📊 Photographer rating: ${averageRating.toFixed(2)} ⭐ (${allReviews.length} total reviews)`);
    console.log(`\n📸 Photographer: ${photographer.user.name}`);
    console.log(`� Clieents: ${createdClients.length} new clients created`);
    console.log(`✨ Reviews with replies: ${reviews.filter(r => r.photographerReply).length}/20`);
    console.log(`\n⭐ Rating distribution:`);
    console.log(`   5 stars: ${reviews.filter(r => r.rating === 5).length}`);
    console.log(`   4 stars: ${reviews.filter(r => r.rating === 4).length}`);
    console.log(`   3 stars: ${reviews.filter(r => r.rating === 3).length}`);

  } catch (error) {
    console.error('❌ Error seeding reviews:', error);
  }
};

// Run the script
const run = async () => {
  await connectDB();
  await seedReviews();
  await mongoose.connection.close();
  console.log('\n👋 Database connection closed');
  process.exit(0);
};

run();
